package day_3

import "github.com/saisona/AdventOfCode2025/helper"

func Solver(path string) int {
	values, err := helper.GetInputToArr(3, path)
	if err != nil {
		panic(err)
	}

	for k, v := range values {
	}
	return 0
}
